name: canari
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Added dependencies
  supabase_flutter: ^2.10.3
  google_maps_flutter: ^2.6.1
  geolocator: ^12.0.0
  google_places_flutter: ^2.0.7
  image_picker: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    # (Optional) Add this if you later include images
    # - assets/images/